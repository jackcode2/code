加typedf,会由变量提升为这种类型。
int(*PFUN)[10];数组指针。
int(*Pfun)(int,int);函数指针
指针和指针相减代表的是这两个指针经历的元素的个数。元素的个数和指针的类型有关
内存有高低地址之分，数据有高低位之别，数据的低位放在高地址处（大端）数据的低权值位放在低地址处（小端）
typedef define const的区别
assert(str!=NULL)断言   在DEBUG环境有作用 宏 不是函数
研究递归实现二分查找
arr在哪里定义就在哪使用
int len=sizeof(arr)/sizeof( arr[0]);
数组是在什么时候确定大小？  编译
什么时候开辟内存   运行
在哪里开辟 栈 最多开辟多少内存 1m或者2m
[]自带解引用 *（arr+i)=   arr[i]=i
结构体传参没有降维。
数组名在sizeof中单独使用，代表整个数组。
取地址数组名，代表整个数组。 
‘0’代表48
\0就是0；
int *p 野指针    预防野指针的方法   ？    初始化或指向Null
putchar（ch-32）ascll码
程序员的自我修养 1，2，3，6，10章
陷进与缺陷
高质量c++
二维数组打印i*col+j
volatile 保持内存的可见性
strstr函数查找字符串              strnact()拼接字符串   三个参数
我们的程序运行会打开三个文件                                   键盘  显示器  显示器
对应的文件指针                                                          stdin  stdout  stderr｛file*｝
                                                                            标准输入 标准输出 标准错误